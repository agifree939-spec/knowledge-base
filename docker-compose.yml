version: "3.8"

services:
  knowledge-base:
    build: .
    restart: unless-stopped
    working_dir: /app
    volumes:
      - kb-data:/data
    environment:
      - KB_DATA_DIR=/data
      - KB_DB_PATH=/data/knowledge.db
      - KB_HOST=0.0.0.0
      - KB_PORT=8501
      - OPENROUTER_API_KEY=tp-s0c2p0mvprccfw8b1vysj1a9rhgezuqjtb4e98n9q3gtlst
      - OPENROUTER_BASE_URL=https://token-plan-sgp.xiaomimimo.com/v1
      - AI_MODEL=mimo-v2.5-pro
      - AUTH_API_KEY=nJQD3B_M8J_valrADSY1D_eA3sW04KJVuCiHslt4fQ4
      - AUTH_DOMAIN=kb.zencore.ccwu.cc
      - AUTH_PROTECTED_PATHS=/api/

volumes:
  kb-data:
    driver: local
