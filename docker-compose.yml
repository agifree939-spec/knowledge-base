version: "3.8"

services:
  knowledge-base:
    build: .
    restart: unless-stopped
    working_dir: /app
    volumes:
      - kb-data:/data
    environment:
      - KB_DATA_DIR=/data
      - KB_DB_PATH=/data/knowledge.db
      - KB_HOST=0.0.0.0
      - KB_PORT=8501
      - OPENROUTER_API_KEY=tp-s0c7vidm4i1mz9ce1ximry6lfke8a4rl0yqxnwxjv5ntdcst
      - OPENROUTER_BASE_URL=https://token-plan-sgp.xiaomimimo.com/v1
      - AI_MODEL=mimo-v2.5-pro
    # Auth Gateway 配置 — API Key 在此查看和修改
    labels:
      - ccwu.auth.api-key=nJQD3B_M8J_valrADSY1D_eA3sW04KJVuCiHslt4fQ4
      - ccwu.auth.domain=kb.zencore.ccwu.cc
      - ccwu.auth.protected-paths=/api/

volumes:
  kb-data:
    driver: local
