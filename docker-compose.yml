version: "3.8"

services:
  knowledge-base:
    build: .
    restart: unless-stopped
    working_dir: /app
    volumes:
      - kb-data:/data
    environment:
      - KB_DATA_DIR=/data
      - KB_DB_PATH=/data/knowledge.db
      - KB_HOST=0.0.0.0
      - KB_PORT=8501
      - OPENROUTER_API_KEY=***    ...[truncated]